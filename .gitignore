node_modules/
dist/
.DS_Store
*.log
.env
.mcp.json

*storybook.log
storybook-static

# Per-run component loop snapshots — regenerated from committed sources by
# `npm run sense:component`. STATUS_QUO.md (the shared baseline) stays committed.
.claude/handoff/
docs/add-component-loop-case-study.html

# Pattern-accuracy harness scratch runs — regenerated by npm run harness:run
scripts/pattern-accuracy-harness/.runs/
