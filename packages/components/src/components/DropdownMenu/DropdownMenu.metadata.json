{
  "component": {
    "name": "DropdownMenu",
    "category": "atom",
    "type": "interactive",
    "status": "beta",
    "description": "A floating panel of selectable items. Shared dropdown primitive used by AppHeader (user menu) and Select (custom listbox). Supports both menu and listbox ARIA roles.",
    "figmaNodeId": "155:6261"
  },
  "usage": {
    "keywords": [
      "dropdown",
      "menu",
      "listbox",
      "options",
      "contextual",
      "popup",
      "select"
    ],
    "when": [
      "Displaying a list of selectable items anchored to a trigger element",
      "Powering a custom Select listbox (listRole=listbox)",
      "Showing a contextual user menu in AppHeader (listRole=menu)"
    ],
    "patterns": [
      {
        "name": "user-context-menu",
        "description": "AppHeader renders DropdownMenu anchored below the user button trigger using absolute positioning.",
        "tree": "AppHeader > div.userWrapper(position:relative) > [button.userButton, DropdownMenu(listRole=menu)]"
      },
      {
        "name": "custom-select",
        "description": "Select renders DropdownMenu as a custom listbox anchored below its trigger button.",
        "tree": "Select > div.wrapper(position:relative) > [button.trigger, DropdownMenu(listRole=listbox)]"
      }
    ],
    "antiPatterns": [
      {
        "scenario": "Using DropdownMenu for navigation with URLs",
        "reason": "Items are <div role='menuitem'>, not anchors. Navigation links need <a> elements for correct semantics.",
        "alternative": "Use a nav element with anchor links for navigation."
      },
      {
        "scenario": "Rendering DropdownMenu outside a position:relative container",
        "reason": "The panel is absolutely positioned and will be offset incorrectly without a positioned ancestor.",
        "alternative": "Always wrap the trigger and DropdownMenu in a position:relative container."
      },
      {
        "scenario": "Composing DropdownMenu without handling initial item focus and trigger focus-return",
        "reason": "All items are tabIndex=-1 by design (single roving focus target, not one Tab stop per item). DropdownMenu does not render its own trigger, so it cannot own focus management — the composing component must programmatically focus the first/selected item when the panel opens and return focus to the trigger when it closes (via onClose), or keyboard/screen-reader users cannot reach the menu or lose their place when it closes.",
        "alternative": "Mirror Select/index.tsx's pattern: a ref to the trigger element, an effect that focuses the first or selected item when the panel opens, and a prevOpenRef-style effect that refocuses the trigger when open transitions from true to false."
      }
    ]
  },
  "variants": {
    "listRole": {
      "options": [
        "menu",
        "listbox"
      ],
      "default": "menu",
      "purpose": {
        "menu": "Contextual action menu. Items get role=menuitem. Use for AppHeader user menu.",
        "listbox": "Form select dropdown. Items get role=option with aria-selected. Use for custom Select."
      }
    }
  },
  "states": [
    {
      "state": "default",
      "description": "Panel visible; items in resting state."
    },
    {
      "state": "item-hover",
      "description": "Neutral background on the hovered or focused item."
    },
    {
      "state": "item-selected",
      "description": "Brand text colour and semibold weight on the currently selected item (listbox role only)."
    }
  ],
  "tokens": {
    "color": [
      "color.background.container.default",
      "color.background.neutral.hover",
      "color.text.default",
      "color.text.brand"
    ],
    "spacing": [
      "space.inset.xs"
    ],
    "typography": [
      "font.family.body",
      "font.size.body-default",
      "font.line-height.none"
    ],
    "borderRadius": [
      "border-radius.sm",
      "border-radius.xs"
    ],
    "other": []
  },
  "accessibility": {
    "role": "menu or listbox (controlled via listRole prop)",
    "ariaAttributes": [
      "role (panel: menu | listbox)",
      "role (items: menuitem | option)",
      "aria-selected (option items when listRole=listbox)"
    ],
    "keyboardInteractions": [
      {
        "key": "Escape",
        "action": "Closes the dropdown and returns focus to the trigger"
      },
      {
        "key": "Enter / Space",
        "action": "Selects the focused item"
      },
      {
        "key": "ArrowUp / ArrowDown",
        "action": "Moves focus between items (single roving focus target — all items are tabIndex=-1; the owning component is responsible for programmatically focusing an item when the panel opens, matching the WAI-ARIA APG menu/listbox pattern)"
      }
    ]
  },
  "composition": {
    "composedOf": [],
    "accepts": [],
    "containedBy": [
      "AppHeader",
      "Select"
    ],
    "layoutBehavior": {
      "widthBehavior": "fixed",
      "displayType": "flex-column",
      "canNest": false
    }
  }
}
