{
  "component": {
    "name": "Box",
    "category": "layout",
    "type": "container",
    "status": "beta",
    "description": "The foundational layout primitive. A polymorphic container that applies spacing tokens via the padding props and controls overflow clipping. Everything else in the system can be placed inside a Box.",
    "figmaNodeId": ""
  },
  "usage": {
    "keywords": [
      "box",
      "container",
      "wrapper",
      "div",
      "padding",
      "spacing",
      "layout",
      "overflow",
      "clip"
    ],
    "when": [
      "Wrapping content that needs padding without imposing flex or grid layout",
      "Providing semantic HTML structure via the as prop (section, article, aside, main)",
      "Creating a padded surface or card-like region before Card is available"
    ],
    "patterns": [
      {
        "name": "padded-surface",
        "description": "A Box wrapping a Stack to create a padded vertical content region.",
        "tree": "Box(padding=lg) > Stack > [Heading, Text, Button]"
      },
      {
        "name": "page-main",
        "description": "Box rendered as the page's <main> landmark — the polymorphic as prop emits a real <main> element. Exactly one per route (ADR-011).",
        "tree": "Box(as='main') > [Box(as='header'), Box(as='section'), Box(as='footer')]"
      },
      {
        "name": "page-header",
        "description": "Box rendered as the page's <header> landmark. Use AppHeader directly when the full header component is available; any child <nav> needs a unique aria-label.",
        "tree": "Box(as='header') > AppHeader"
      },
      {
        "name": "page-section",
        "description": "Box rendered as a <section> landmark. aria-labelledby must point to the visible Heading id; use aria-label only when there is no visible heading. Add paddingY for vertical rhythm. A .container div inside handles max-width and grid margins.",
        "tree": "Box(as='section', aria-labelledby='section-heading-id', paddingY='xl') > [div.container > ...]"
      },
      {
        "name": "page-footer",
        "description": "Box rendered as the page's <footer> (contentinfo landmark). One per page; a .container div inside handles max-width and grid margins. Pair background='inverted' with borderTop='inverted' for a footer that separates from an inverted-background section above it.",
        "tree": "Box(as='footer', background='inverted', borderTop='inverted') > [div.container > ...]"
      },
      {
        "name": "container",
        "description": "Box with className='container' to center content in a section. The .container global class applies max-width + grid margin (packages/components/src/styles/grid.css). Prefer this over a raw <div className='container'>.",
        "tree": "Box(className='container') > Stack > [...]"
      }
    ],
    "antiPatterns": [
      {
        "scenario": "Using Box to apply flex or grid layout",
        "reason": "Box has no layout opinions beyond padding. It is not a flex container.",
        "alternative": "Use Stack for vertical flex layout or Inline for horizontal flex layout"
      },
      {
        "scenario": "Nesting Boxes purely to add padding when a single Box with paddingX/paddingY suffices",
        "reason": "Unnecessary nesting inflates the DOM and makes spacing harder to reason about",
        "alternative": "Use the paddingX and paddingY props on a single Box to apply asymmetric spacing"
      },
      {
        "scenario": "Using a raw <div className='container'> instead of Box",
        "reason": "The grammar requires all structural elements to go through the component system. A raw div bypasses the as-prop pattern and makes the tree inconsistent.",
        "alternative": "Use <Box className='container'> or <div className='container'> only as a presentational wrapper inside a Box landmark — never as a replacement for a landmark Box"
      },
      {
        "scenario": "Setting color via inline style on Box or a child span",
        "reason": "Inline color styles bypass the token system and break dark-mode switching.",
        "alternative": "Use <Text color='…'> or <Heading> with a token-backed color prop. Apply the semantic color token on the element that owns the color context, not on Box."
      },
      {
        "scenario": "Hand-writing style={{ flex: '1 0 0' }}, style={{ minWidth }}, style={{ maxWidth }}, style={{ minHeight }}, or style={{ maxHeight }} on a Box for column fill or size constraints",
        "reason": "Box has first-class grow/minWidth/maxWidth/minHeight/maxHeight props that emit the same CSS through the component's own style resolution — inline style duplicates this and drifts from it over time.",
        "alternative": "Use the grow prop (flex: n 0 0) for column fill, and minWidth/maxWidth/minHeight/maxHeight props for size constraints, instead of an inline style object."
      },
      {
        "scenario": "Hand-writing style={{ background: 'var(--ds-color-background-container-elevated)', borderRadius: '4px' }} on a Box",
        "reason": "Duplicates the background variant's own resolution with a raw radius literal instead of a token reference — drifts the moment the radius scale or elevated background alias changes.",
        "alternative": "Use background='elevated' on Box."
      },
      {
        "scenario": "Hand-writing style={{ borderTop: '1px solid var(--ds-color-border-inverted)' }} on a Box",
        "reason": "Duplicates the borderTop variant's own resolution as a raw CSS shorthand string instead of a resolved prop — the same border rule was hand-copied across three page footers before being promoted to a prop.",
        "alternative": "Use borderTop='inverted' (or borderTop='default') on Box."
      }
    ]
  },
  "variants": {
    "variant": {
      "options": [
        "default"
      ],
      "default": "default",
      "purpose": {
        "default": "A generic padded container. Rendered as div by default; override with the as prop for semantic HTML."
      }
    },
    "background": {
      "options": [
        "canvas",
        "inverted",
        "transparent",
        "elevated"
      ],
      "default": null,
      "purpose": {
        "canvas": "Flat surface matching the page background — same fill as Card's default variant, for a Box standing in as a plain surface or as the page's own wrapper.",
        "inverted": "Dark surface for CTA banners, footers, or other sections that invert against the page's default theme.",
        "transparent": "Explicitly no fill — lets the parent surface show through.",
        "elevated": "Raised surface with a token-backed radius, for a Box standing in as a card-like region (e.g. Storybook demo chrome) before Card is warranted."
      }
    },
    "borderTop": {
      "options": [
        "default",
        "inverted"
      ],
      "default": null,
      "purpose": {
        "default": "A subtle top border for separating a Box from the content above it on the page's default theme.",
        "inverted": "A top border for separating an inverted-background Box (e.g. a dark footer) from the section above it."
      }
    }
  },
  "states": [
    {
      "state": "default",
      "description": "Static container — no interactive states."
    }
  ],
  "tokens": {
    "color": [
      "color.background.container.canvas",
      "color.background.container.inverted",
      "color.background.container.elevated",
      "color.border.default",
      "color.border.inverted"
    ],
    "borderRadius": [
      "border-radius.xs"
    ],
    "spacing": [
      "space.inset.xxs",
      "space.inset.xs",
      "space.inset.sm",
      "space.inset.md",
      "space.inset.lg",
      "space.inset.xl",
      "space.inset.xxl",
      "space.inset.xxxl"
    ]
  },
  "accessibility": {
    "role": "generic",
    "ariaAttributes": [],
    "keyboardInteractions": []
  },
  "composition": {
    "composedOf": [],
    "accepts": [
      "Box",
      "Stack",
      "Inline",
      "Text",
      "Heading",
      "Icon",
      "Button",
      "TextField",
      "Select",
      "Checkbox",
      "Card"
    ],
    "containedBy": [
      "Box",
      "Stack",
      "Inline",
      "Card"
    ],
    "layoutBehavior": {
      "widthBehavior": "fill",
      "displayType": "block",
      "canNest": true
    }
  }
}
