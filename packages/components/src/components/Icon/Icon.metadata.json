{
  "component": {
    "name": "Icon",
    "category": "atom",
    "type": "display",
    "status": "beta",
    "description": "Renders one of 20 inline SVGs from the fixed icon set. Uses currentColor so it inherits the surrounding text color. Size is controlled via size.icon.* tokens.",
    "figmaNodeId": "96:11108"
  },
  "usage": {
    "keywords": [
      "icon",
      "svg",
      "glyph",
      "symbol",
      "pictogram"
    ],
    "when": [
      "Providing a visual cue alongside a label (Button leading icon, form field prefix)",
      "Indicating direction or action in a compact space (chevrons, plus, minus)",
      "Communicating theme state (sun/moon-star for light/dark toggle)"
    ],
    "patterns": [
      {
        "name": "icon-label-pair",
        "description": "An Icon followed by a Text label inside an Inline, used for navigation items and feature callouts.",
        "tree": "Inline(gap=xs, align=center) > [Icon(size=md), Text(size=body-default)]"
      }
    ],
    "antiPatterns": [
      {
        "scenario": "Using Icon as the sole accessible label for an interactive control",
        "reason": "aria-hidden=true by default; a visually icon-only button has no accessible name.",
        "alternative": "Pass the label prop to set aria-label, or wrap in a Button with a visually-hidden text label."
      },
      {
        "scenario": "Adding icons outside the fixed set",
        "reason": "The set is intentionally small. Adding arbitrary icons bloats the bundle and fragments the visual language.",
        "alternative": "Choose the closest existing icon, or propose expanding the fixed set deliberately."
      },
      {
        "scenario": "Setting a color prop or inline style directly on the Icon element",
        "reason": "Icon uses currentColor — color must be set on the ancestor element that owns the color context, using a semantic token. IconProps currently only omits 'children' from the spread SVGAttributes type, so color, fill, stroke, strokeWidth, role, aria-hidden, and aria-label can all be passed through and silently override Icon's intended behavior with no type error.",
        "alternative": "Apply the token via style or CSS on the ancestor span/element, never on <Icon> itself. Token choice depends on context: use color.icon.default for standalone or button-adjacent icons; use color.icon.subtle when the icon appears inline alongside color.text.subtle or metadata-scale text, so the icon sits in the same visual register as its label."
      }
    ]
  },
  "variants": {
    "size": {
      "options": [
        "sm",
        "md"
      ],
      "default": "md",
      "purpose": {
        "sm": "16px — use inside dense UI elements such as tags, badges, or inline metadata.",
        "md": "24px — default; use alongside body text, in buttons, and as standalone affordances."
      }
    }
  },
  "states": [
    {
      "state": "default",
      "description": "Static — no interactive states on the icon itself."
    }
  ],
  "tokens": {
    "other": [
      "size.icon.sm",
      "size.icon.md"
    ]
  },
  "accessibility": {
    "role": "none (aria-hidden) by default; img when label is provided",
    "ariaAttributes": [
      "aria-hidden (default true)",
      "aria-label (when label prop provided)",
      "role=img (when label prop provided)"
    ],
    "keyboardInteractions": [],
    "notes": "Icons are decorative by default (aria-hidden=true). When an Icon is the only content conveying meaning, pass the label prop to add aria-label and role=img."
  },
  "composition": {
    "composedOf": [],
    "accepts": [],
    "containedBy": [
      "Box",
      "Stack",
      "Inline",
      "Button",
      "Card",
      "ButtonArrow",
      "Accordion",
      "TextField",
      "CardHorizontal",
      "CardVertical",
      "Image",
      "AppHeader",
      "Select",
      "Breadcrumb"
    ],
    "layoutBehavior": {
      "widthBehavior": "fixed",
      "displayType": "inline",
      "canNest": false
    }
  }
}
