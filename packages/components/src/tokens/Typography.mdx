import { Meta } from '@storybook/addon-docs/blocks'
import { FontSizeTable, FontWeightTable, LineHeightTable } from './tokenComponents'

<Meta title="Foundation/Typography" />

# Typography

The type scale has 11 steps from `font.size.1` (0.75rem / 12px) to `font.size.11` (2rem / 32px), all in rem so they scale with the user's browser font size preference. Line-heights are unitless ratios — never fixed px values.

## Font Size

<FontSizeTable />

## Font Weight

<FontWeightTable />

## Line Height

Line heights are unitless ratios. Use lower values for large display text (tighter optical fit), higher values for body copy (readability).

<LineHeightTable />
