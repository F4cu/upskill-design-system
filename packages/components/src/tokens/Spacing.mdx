import { Meta } from '@storybook/addon-docs/blocks'
import { SpacingTable, SemanticSpacingTable } from './tokenComponents'

<Meta title="Foundation/Spacing" />

# Spacing

The spacing scale is built on a **4px base unit** (`space050 = 0.25rem`). All steps are multiples of 4, giving consistent vertical rhythm and alignment across layouts. Use tokens rather than raw values — this makes global layout adjustments a one-line change.

## Scale

<SpacingTable />

## Semantic spacing

Semantic tokens assign intent to primitive values. Use these in components and layouts rather than raw scale steps — they carry meaning and respond to breakpoints. Tablet and mobile values highlighted in <span style={{color:'#d15d50'}}>red</span> differ from desktop.

Breakpoints: **desktop** ≥ 1440 px · **tablet** ≥ 768 px · **mobile** < 768 px.

### Inline

Horizontal gap between adjacent inline elements — icon-to-label distance, chip gaps, button-group spacing.

<SemanticSpacingTable category="inline" />

### Stack

Vertical gap between stacked block elements — form fields, list items, card grids, section separators.

<SemanticSpacingTable category="stack" />

### Inset

Padding inside a container — card padding, button padding, panel or dialog inset.

<SemanticSpacingTable category="inset" />

## Usage guidance

| Use case | Token |
|---|---|
| Icon padding, tight inline gaps | `space050` – `space100` |
| Component internal padding | `space150` – `space300` |
| Section padding, card gaps | `space400` – `space500` |
| Page-level vertical rhythm | `space600` – `space800` |

Avoid skipping more than two steps between adjacent elements — it creates visual imbalance. When in doubt, use the next step up rather than a custom value.
